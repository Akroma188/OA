%% 4.2 and 4.3
%



[x_old, u_old, count] = p1_variationC(1);

[x_old, u_old] = p1_variationC_weights(x_old, u_old, 1);
[x_old, u_old] = p1_variationC_weights(x_old, u_old, 2);
[x_old, u_old] = p1_variationC_weights(x_old, u_old, 3);
[x_old, u_old] = p1_variationC_weights(x_old, u_old, 4);
[x_old, u_old] = p1_variationC_weights(x_old, u_old, 5);
[x_old, u_old] = p1_variationC_weights(x_old, u_old, 6);

